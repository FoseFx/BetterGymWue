---
kind: pipeline
name: default

steps:
  - name: build container
    image: docker
    volumes:
      - name: dockersock
        path: /var/run/docker.sock
    commands:
      - docker build -t fosefx/bettergymwue-backend:${DRONE_COMMIT_SHA} backend

volumes:
  - name: dockersock
    host:
      path: /var/run/docker.sock

trigger:
  branch:
    - master
